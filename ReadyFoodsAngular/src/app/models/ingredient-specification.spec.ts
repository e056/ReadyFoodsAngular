import { IngredientSpecification } from './ingredient-specification';

describe('IngredientSpecification', () => {
  it('should create an instance', () => {
    expect(new IngredientSpecification()).toBeTruthy();
  });
});
